## 1. Update XML source

- [x] 1.1 Change `<shortDescription>` from `winkeys-p` to `ru-pr` in `evdev.extras.xml` (line 896)

## 2. Update patch file

- [x] 2.1 Update the corresponding line in `0001-keyboards-list.patch` to reflect `ru-pr`

## 3. Update spec

- [x] 3.1 Update `openspec/specs/req-01-winkeys-pd-registration/spec.md` — change shortDescription from `"winkeys-p"` to `"ru-pr"`
