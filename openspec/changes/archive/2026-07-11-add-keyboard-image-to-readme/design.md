## Context

Файл `readme.md` описывает раскладку `winkeys-pd`, но не содержит визуальной схемы. Картинка `keyboard.png` уже лежит в корне проекта.

## Goals / Non-Goals

**Goals:**
- Показать читателю визуальную схему раскладки сразу в README

**Non-Goals:**
- Изменять структуру README
- Добавлять alt-текст или подпись

## Decisions

**Размещение:** После абзаца описания (строка 7), перед подразделом `### Изменённые клавиши`.

Альтернативы:
- В начало раздела — отодвигает описание
- После таблицы — не логично, таблица — детали
- Между описанием и таблицей —最佳, читатель сначала видит картинку, потом детали

**Синтаксис:** `![Раскладка winkeys-pd](keyboard.png)` — стандартный markdown, относительный путь.

## Risks / Trade-offs

- Нет рисков. Одна правка, один файл.
